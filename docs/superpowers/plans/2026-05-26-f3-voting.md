# F3 机会投票 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为每条机会卡片底部添加 👍/👎 投票按钮，计数存储在 Upstash Redis，支持乐观更新，localStorage 防同浏览器重复投票。

**Architecture:** `OpportunityList` 调用 `useVotes(date, ids)` 批量拉取当天所有机会的投票计数，将 `initialCounts` 传给每个 `VoteBar`。`VoteBar` 管理本地乐观更新状态：点击后立即 +1，`POST /api/vote` 成功后用服务端计数替换，失败时回滚。Vercel KV 使用 Upstash Redis，键结构为 `votes:{date}:{id}` → Hash `{ up, down }`。

**Tech Stack:** Next.js 14 App Router, `@upstash/redis`, TypeScript, React hooks, localStorage

---

### Task 1: 安装依赖，创建 Redis 客户端

**Files:**
- Create: `lib/kv.ts`

- [ ] **Step 1: 安装 @upstash/redis**

```bash
cd /Users/sanli/Desktop/workplace/ai-programme/workspace/projects/opportunity-radar
npm install @upstash/redis
```

Expected: package added to `package.json`, no errors.

- [ ] **Step 2: 确认 .env.local 有以下两个变量**

打开 `.env.local`，确认存在：
```
KV_REST_API_URL="https://calm-moose-136731.upstash.io"
KV_REST_API_TOKEN="gQAAAAAAAhYb..."
```

这两个变量在 Vercel 已通过 Marketplace 注入，本地开发需要手动在 `.env.local` 配置。

- [ ] **Step 3: 创建 `lib/kv.ts`**

```typescript
import { Redis } from '@upstash/redis'

export const kv = new Redis({
  url: process.env.KV_REST_API_URL!,
  token: process.env.KV_REST_API_TOKEN!,
})
```

- [ ] **Step 4: Commit**

```bash
git add lib/kv.ts package.json package-lock.json
git commit -m "feat: add Upstash Redis client for voting"
```

---

### Task 2: POST /api/vote 端点

**Files:**
- Create: `app/api/vote/route.ts`

- [ ] **Step 1: 创建 `app/api/vote/route.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { kv } from '@/lib/kv'

export async function POST(req: NextRequest) {
  try {
    const body = await req.json()
    const { date, id, type } = body

    if (
      typeof date !== 'string' ||
      typeof id !== 'number' ||
      (type !== 'up' && type !== 'down')
    ) {
      return NextResponse.json({ error: '参数错误' }, { status: 400 })
    }

    const key = `votes:${date}:${id}`
    await kv.hincrby(key, type, 1)
    const result = await kv.hgetall<{ up: string; down: string }>(key)

    return NextResponse.json({
      up: parseInt(result?.up ?? '0', 10),
      down: parseInt(result?.down ?? '0', 10),
    })
  } catch (err) {
    console.error('vote error:', err)
    return NextResponse.json({ error: '服务器错误' }, { status: 500 })
  }
}
```

- [ ] **Step 2: 启动 dev server 并测试**

```bash
npm run dev
```

新开终端，运行：
```bash
curl -s -X POST http://localhost:3000/api/vote \
  -H "Content-Type: application/json" \
  -d '{"date":"2026-05-26","id":1,"type":"up"}' | jq
```

Expected:
```json
{ "up": 1, "down": 0 }
```

再执行一次，expected `{ "up": 2, "down": 0 }`。

- [ ] **Step 3: 测试错误校验**

```bash
curl -s -X POST http://localhost:3000/api/vote \
  -H "Content-Type: application/json" \
  -d '{"date":"2026-05-26","id":"bad","type":"up"}' | jq
```

Expected: `{ "error": "参数错误" }` with status 400.

- [ ] **Step 4: Commit**

```bash
git add app/api/vote/route.ts
git commit -m "feat: add POST /api/vote endpoint"
```

---

### Task 3: GET /api/votes 批量读取端点

**Files:**
- Create: `app/api/votes/route.ts`

- [ ] **Step 1: 创建 `app/api/votes/route.ts`**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { kv } from '@/lib/kv'

export async function GET(req: NextRequest) {
  try {
    const { searchParams } = new URL(req.url)
    const date = searchParams.get('date')
    const idsParam = searchParams.get('ids')

    if (!date || !idsParam) {
      return NextResponse.json({ error: '参数错误' }, { status: 400 })
    }

    const ids = idsParam.split(',').map(Number).filter(n => !isNaN(n) && n > 0)
    if (ids.length === 0) {
      return NextResponse.json({})
    }

    const pipeline = kv.pipeline()
    for (const id of ids) {
      pipeline.hgetall(`votes:${date}:${id}`)
    }
    const results = await pipeline.exec()

    const response: Record<string, { up: number; down: number }> = {}
    ids.forEach((id, i) => {
      const raw = results[i] as { up?: string; down?: string } | null
      response[String(id)] = {
        up: parseInt(raw?.up ?? '0', 10),
        down: parseInt(raw?.down ?? '0', 10),
      }
    })

    return NextResponse.json(response)
  } catch (err) {
    console.error('votes error:', err)
    return NextResponse.json({ error: '服务器错误' }, { status: 500 })
  }
}
```

- [ ] **Step 2: 测试批量读取（需要 dev server 在运行）**

```bash
curl -s "http://localhost:3000/api/votes?date=2026-05-26&ids=1,2,3" | jq
```

Expected（id=1 刚才投了 2 票，2 和 3 为 0）：
```json
{
  "1": { "up": 2, "down": 0 },
  "2": { "up": 0, "down": 0 },
  "3": { "up": 0, "down": 0 }
}
```

- [ ] **Step 3: Commit**

```bash
git add app/api/votes/route.ts
git commit -m "feat: add GET /api/votes batch endpoint"
```

---

### Task 4: useVotes hook

**Files:**
- Create: `hooks/useVotes.ts`

- [ ] **Step 1: 创建 `hooks/useVotes.ts`**

```typescript
'use client'

import { useState, useEffect } from 'react'

export type VoteCounts = { up: number; down: number }

export function useVotes(
  date: string,
  ids: number[],
): Record<number, VoteCounts> {
  const [counts, setCounts] = useState<Record<number, VoteCounts>>({})
  const idsKey = ids.join(',')

  useEffect(() => {
    if (!idsKey) return
    fetch(`/api/votes?date=${encodeURIComponent(date)}&ids=${idsKey}`)
      .then(r => (r.ok ? r.json() : null))
      .then((data: Record<string, { up: number; down: number }> | null) => {
        if (!data) return
        const mapped: Record<number, VoteCounts> = {}
        for (const [key, val] of Object.entries(data)) {
          mapped[Number(key)] = val
        }
        setCounts(mapped)
      })
      .catch(() => {})
  }, [date, idsKey])

  return counts
}
```

- [ ] **Step 2: Commit**

```bash
git add hooks/useVotes.ts
git commit -m "feat: add useVotes hook for batch vote count fetching"
```

---

### Task 5: VoteBar 组件

**Files:**
- Create: `components/VoteBar.tsx`

- [ ] **Step 1: 创建 `components/VoteBar.tsx`**

```typescript
'use client'

import { useState, useEffect } from 'react'
import type { VoteCounts } from '@/hooks/useVotes'

interface Props {
  date: string
  opportunityId: number
  initialCounts?: VoteCounts
}

export default function VoteBar({ date, opportunityId, initialCounts }: Props) {
  const [counts, setCounts] = useState<VoteCounts>({ up: 0, down: 0 })
  const [voted, setVoted] = useState<'up' | 'down' | null>(null)
  const [loading, setLoading] = useState(false)

  // 当 useVotes 在父组件拉取完成后，更新本地 counts（仅未投票时）
  useEffect(() => {
    if (initialCounts && !voted) {
      setCounts(initialCounts)
    }
  }, [initialCounts?.up, initialCounts?.down]) // eslint-disable-line react-hooks/exhaustive-deps

  // 从 localStorage 恢复已投状态
  useEffect(() => {
    const stored = localStorage.getItem(`voted:${date}:${opportunityId}`)
    if (stored === 'up' || stored === 'down') setVoted(stored)
  }, [date, opportunityId])

  async function handleVote(type: 'up' | 'down') {
    if (voted || loading) return

    // 乐观更新
    setCounts(prev => ({ ...prev, [type]: prev[type] + 1 }))
    setVoted(type)
    localStorage.setItem(`voted:${date}:${opportunityId}`, type)
    setLoading(true)

    try {
      const res = await fetch('/api/vote', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ date, id: opportunityId, type }),
      })
      if (!res.ok) throw new Error('vote failed')
      const data: VoteCounts = await res.json()
      setCounts(data)
    } catch {
      // 回滚
      setCounts(prev => ({ ...prev, [type]: prev[type] - 1 }))
      setVoted(null)
      localStorage.removeItem(`voted:${date}:${opportunityId}`)
    } finally {
      setLoading(false)
    }
  }

  const isDisabled = voted !== null || loading

  function btnStyle(type: 'up' | 'down') {
    const isVoted = voted === type
    const isOther = voted !== null && voted !== type
    return {
      background: isVoted ? (type === 'up' ? '#f0fdf4' : '#fef2f2') : isOther ? '#f9fafb' : type === 'up' ? '#f0fdf4' : '#fef2f2',
      color: isVoted ? (type === 'up' ? '#16a34a' : '#dc2626') : isOther ? '#9ca3af' : type === 'up' ? '#16a34a' : '#dc2626',
      borderRadius: '20px',
      padding: '7px 16px',
      fontSize: '14px',
      fontWeight: 600,
      border: 'none',
      cursor: isDisabled ? 'default' : 'pointer',
      opacity: isOther ? 0.5 : 1,
      transition: 'all 0.15s',
    } as React.CSSProperties
  }

  return (
    <div className="flex items-center gap-2 pt-3 border-t border-r-border mt-3">
      <span className="font-mono text-[11px] text-r-faint tracking-[0.15em] uppercase flex-1">
        对你有帮助吗？
      </span>
      <button onClick={() => handleVote('up')} disabled={isDisabled} style={btnStyle('up')}>
        👍 {counts.up > 0 ? counts.up : ''}
      </button>
      <button onClick={() => handleVote('down')} disabled={isDisabled} style={btnStyle('down')}>
        👎 {counts.down > 0 ? counts.down : ''}
      </button>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add components/VoteBar.tsx
git commit -m "feat: add VoteBar component with optimistic update"
```

---

### Task 6: 接入 OpportunityCard、OpportunityList、页面

**Files:**
- Modify: `components/OpportunityCard.tsx`
- Modify: `components/OpportunityList.tsx`
- Modify: `app/[date]/page.tsx`

- [ ] **Step 1: 修改 `components/OpportunityCard.tsx`**

在文件顶部 import 处添加：

```typescript
import VoteBar from './VoteBar'
import type { VoteCounts } from '@/hooks/useVotes'
```

将组件签名从：
```typescript
export default function OpportunityCard({
  opportunity: o,
  index,
}: {
  opportunity: Opportunity
  index: number
})
```

改为：
```typescript
export default function OpportunityCard({
  opportunity: o,
  index,
  date,
  initialCounts,
}: {
  opportunity: Opportunity
  index: number
  date: string
  initialCounts?: VoteCounts
})
```

在组件 body 最后（`</div>` 结束 article 之前），找到 Sources 区块末尾的闭合 `</div>`（body div 的最后），在其后、article 闭合 `</article>` 之前插入 VoteBar：

找到：
```typescript
        {/* Sources */}
        {o.sources.length > 0 && (
          <div className="flex flex-wrap gap-3">
            {o.sources.map((s, i) => {
```

在整个 sources 块（含其最终闭合 `</div>`）之后、body div 的闭合 `</div>` 之前，插入：

```typescript
        <VoteBar date={date} opportunityId={o.id} initialCounts={initialCounts} />
```

完整的 body div 末尾应为：
```typescript
        {/* Sources */}
        {o.sources.length > 0 && (
          <div className="flex flex-wrap gap-3">
            {/* ... existing sources code ... */}
          </div>
        )}

        <VoteBar date={date} opportunityId={o.id} initialCounts={initialCounts} />
      </div>
    </article>
  )
```

- [ ] **Step 2: 修改 `components/OpportunityList.tsx`**

在文件顶部 import 处添加：
```typescript
import { useVotes } from '@/hooks/useVotes'
```

将 `Props` interface 从：
```typescript
interface Props {
  opportunities: Opportunity[]
}
```

改为：
```typescript
interface Props {
  opportunities: Opportunity[]
  date: string
}
```

将组件签名从：
```typescript
export default function OpportunityList({ opportunities }: Props) {
```

改为：
```typescript
export default function OpportunityList({ opportunities, date }: Props) {
```

在组件内部，`useState` hooks 之后添加：
```typescript
const ids = useMemo(() => opportunities.map(o => o.id), [opportunities])
const voteCounts = useVotes(date, ids)
```

（`useMemo` 已在文件顶部导入，确认存在）

将 `OpportunityCard` 的渲染从：
```typescript
<OpportunityCard key={opp.id} opportunity={opp} index={i} />
```

改为：
```typescript
<OpportunityCard
  key={opp.id}
  opportunity={opp}
  index={i}
  date={date}
  initialCounts={voteCounts[opp.id]}
/>
```

- [ ] **Step 3: 修改 `app/[date]/page.tsx`**

找到：
```typescript
<OpportunityList opportunities={data.opportunities} />
```

改为：
```typescript
<OpportunityList opportunities={data.opportunities} date={params.date} />
```

- [ ] **Step 4: Commit**

```bash
git add components/OpportunityCard.tsx components/OpportunityList.tsx app/[date]/page.tsx
git commit -m "feat: wire VoteBar into opportunity cards"
```

---

### Task 7: 端到端测试 & 推送

- [ ] **Step 1: 启动 dev server**

```bash
npm run dev
```

打开 `http://localhost:3000`（会跳转到最新日期页面）。

- [ ] **Step 2: 检查投票按钮渲染**

每张机会卡片底部应看到：
```
对你有帮助吗？  [👍]  [👎]
```

两个彩色胶囊按钮（👍 绿色背景，👎 红色背景），初始无数字。

- [ ] **Step 3: 测试投票交互**

1. 点击某张卡片的 👍 → 立即变为 `👍 1`，👎 变灰
2. 刷新页面 → 该按钮仍为已投状态（localStorage 恢复）
3. 另一张卡片的 👎 也可以独立投票
4. 打开 Upstash 控制台（`https://console.upstash.com`），Data Browser 里确认存在 `votes:YYYY-MM-DD:1` 等 key

- [ ] **Step 4: 检查 localStorage**

浏览器 DevTools → Application → Local Storage：
应看到 `voted:2026-05-26:1 = "up"` 等条目。

- [ ] **Step 5: 推送并验证 Vercel 部署**

```bash
git push
```

等 Vercel 部署完成后，打开 `opradar.indevs.in`，重复以上测试步骤，确认线上环境正常（Vercel 已自动注入 KV 环境变量）。
